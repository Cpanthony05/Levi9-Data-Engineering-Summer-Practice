version: 2

models:
  - name: fct_trips
    description: "fact trips"
    columns:
      - name: trip_id
        description: "The primary key for this table"
        data_tests:
          - unique
          - not_null
